{
  "name": "repro",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "test:clean": "rm -rf node_modules/.vite && yarn test",
    "test": "web-test-runner \"test/example.js\""
  },
  "devDependencies": {
    "@vitejs/plugin-react-refresh": "*",
    "@web/test-runner": "*",
    "assert": "*",
    "mocha": "*",
    "vite": "*",
    "vite-web-test-runner-plugin": "*"
  }
}
